# Rooted-Panel-Android-Studio
This Android Studio project is made for rooted devices only. It uses root (su) access to perform advanced system-level operations like accessing protected directories, copying or modifying system files, executing root commands, and automating tasks not possible on non-rooted devices
